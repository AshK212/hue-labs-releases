# Python
backend/.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Local app data (benchmark history, etc.)
backend/data/*.db
backend/data/*.json
!backend/data/.gitkeep

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.tsbuild/
frontend/*.tsbuildinfo

# Electron desktop shell — build outputs
node_modules/
dist-electron/
release/

# Bundled backend (PyInstaller output)
backend/dist/
backend/build/

# Env / secrets
.env
*.local

# Generated packaged production config (carries the release API key at build
# time). Written by scripts/generate_production_config.js and bundled as an
# Electron extraResource. Never commit it.
production-config/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

.env